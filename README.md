# Trading Journal

This repository contains a simple React Native project built with [Expo](https://expo.dev).

The source code lives in the **BasicApp** directory.

## Running the app

1. Install dependencies:
   ```bash
   cd BasicApp
   npm install
   ```
2. Start the development server:
   ```bash
   npx expo start
   ```
